Raw downloads cached here by `data/fetch_carbon_intensity.py` and
`data/fetch_gpu_pricing.py` (multi-MB, gitignored). Regenerate with:

```
uv run python -m data.fetch_carbon_intensity
uv run python -m data.fetch_gpu_pricing
```
